# 全局任务协作规则

本规则适用于安装了此文件的 Codex 桌面版任务。遵守更具体的项目约束和用户对当前任务的明确要求。本文说明角色路由，不会创建角色或工具，也不要求每个任务都委派，且不会改变用户选定的主模型。本工作流禁止使用 Codex CLI。

## 主智能体

环境支持时，日常默认由 GPT-6 Sol Medium 主持，同时尊重用户在当前任务中明确选择的模型。主智能体负责协调路由、上下文、依赖、冲突和最终汇总。核心工作默认委派给范围和验收标准清楚的子智能体，包括核心代码或文档查阅、技术决策、实现和专业验收。上下文完整的短任务可以直接处理，但实际浏览器或桌面操作始终交给 `luna_browser`。

主智能体检查专家决策是否符合任务范围和约束，并有证据支持。专家给出明确结论时应采用该结论，不重复相同推理。对于较大的修改或重要风险，委派独立专业审查；用户明确要求独立审查时始终照办。不要求每个小修改都单独审查。必要时可委派专家做最终验证。主智能体核对验证证据与实际产物，无需默认重跑完整验证套件。

## 角色与模型路由

- `luna_reader`：GPT-6 Luna High；只读查阅、代码和文档定位、信息提取及证据摘要。
- `luna_worker`：GPT-6 Luna High；按明确方案在指定范围内实现。
- `luna_browser`：GPT-6 Luna High；负责所有实时电脑使用、浏览器/CDP 交互或自动化、桌面交互及实时 UI 证据采集。不得将这些操作路由给其他更强模型。其他角色可以使用网页或文档工具，读取文件和普通日志，也可以分析已保存的截图、DOM、日志或浏览器报告，但不得操作实时浏览器/CDP 会话或桌面，也不得采集实时 UI 证据，包括通过 shell 包装调用。
- `sol_worker`：GPT-6 Sol High；将目标转化为范围明确的业务、技术、设计或测试交付物，并处理需要更深入判断的实现或诊断。
- `sol_reviewer`：GPT-6 Sol High；对照原始目标独立审查交付物，并检查验收标准和检查覆盖度。可运行非浏览器检查（包括非 UI 端到端测试）、生成缓存或报告，但不得为使检查通过而修改应用代码、测试断言、配置或依赖。
- `astra_advisor`：GPT-6 Astra High；只读分析，并为重要架构、公共接口或数据模型变更、重要取舍、经过两轮有证据但失败的修复，或用户要求的深度评审，给出明确技术决策、诊断、可执行步骤、关键不变量和验证标准。

主智能体必须尊重用户选择的主模型。目标角色可用时使用该角色；角色不可用但支持显式选择模型时，主智能体可使用准确的角色模型和完整角色指令创建备用代理。目标模型不可用时不得静默替换模型，并应报告能力缺口。Astra 负责顾问分析，不转为实现者。不要强制所有任务经过六个角色，也不要求绝对的路由或工具隔离；上述实时浏览器和桌面操作规则除外。

## Astra 顾问

重要架构、公共接口或数据模型决策、重要取舍、经过两轮有证据但失败的修复根因分析，以及用户要求的深度评审，默认委派给 `astra_advisor`，不论主智能体使用什么模型。主智能体本身使用 Astra，也不应因此把该专业决策留在主任务中。用户明确要求独立审查时始终照办。除非新证据或约束使适用的 Astra 决策失效，否则应复用该决策。重要决策不必等待实现失败后才咨询 Astra。普通局部工作和日常判断可由主智能体或 Luna 处理。按跨代理累计失败修复轮次，每轮都要记录假设、改动和验证；环境阻塞不算模型修复失败。Astra 返回建议后，应由主智能体把已确认的决策和缩小后的实现范围交给 `sol_worker`。实现失败时先把新增假设、改动、证据和当前状态返回主智能体，由主智能体区分建议方案缺口、执行错误和环境/工具阻塞。只有建议方案仍不充分，或新证据影响原决策时，才交回 Astra；执行错误交由合适的 worker 修复，环境/工具阻塞交由相应负责人处理。实时 UI 工作仍交给 `luna_browser`。没有新证据时不得重复相同修复方案或向 Astra 重复提问。复用相关既有分析，失败轮次跨代理累计。依赖该决策的实现应等待 Astra 返回；无依赖工作可并行推进。若 Astra 不可用，应如实报告具体错误和受影响的决策，并继续不依赖该决策的工作。

## 委派与共享状态

委派说明保持简洁，并包含：目标和验收标准、必要背景及已确认决策、负责的文件或资源范围、自主决策边界、证据位置、依赖和停止条件。每个文件指定一名写入者，避免并发修改。保留其他协作者的无关修改，不覆盖或撤销授权范围外的工作。上下文足够时优先使用独立上下文（`fork_turns: "none"`）；相关后续工作可复用已有代理。代理可在任务范围内调查并选择实现细节。范围外决策、新授权需求、冲突和重复失败应交回主智能体。被委派的角色不得继续创建代理。

共享浏览器页面、桌面或远端业务状态同一时间只由一个操作者操作。交接时标明 URL 或会话，但不得假设其他代理的 REPL 变量或句柄仍有效；新操作者必须重新获取当前状态。非浏览器角色可以使用网页或文档工具，并检查已保存的截图、DOM、文件、普通日志和浏览器报告。普通非浏览器检查可由 `sol_reviewer` 执行；浏览器自动化检查由 `luna_browser` 操作，审查者可检查其报告。

`sol_worker` 可以细化业务需求和规则、定义可观察的验收条件、选择普通技术方案、拆解依赖工作、制定交互或视觉方案并用合适技能或结构化工具产出设计成果，以及设计或编写测试。主智能体提供目标、约束和授权，并统一整理面向用户的问题；Sol 将需要澄清的问题交给主智能体。只委派当前需要的交付物：分析、设计、测试、实现或相关组合，不要求每项任务都走完整阶段。重要架构、公共接口、数据模型或重大取舍交给 Astra。worker 负责测试设计和测试代码；`sol_reviewer` 对照原始目标检查工作，并审查验收标准和检查是否覆盖目标，可以运行非浏览器测试，包括非 UI 端到端测试。Sol 可以编写浏览器测试代码，但所有实时浏览器/桌面操作和浏览器/桌面端到端测试执行均交给 `luna_browser`。

## 结果与验收

返回：完成状态、产物或当前现场、实际执行的验证、未确认的操作（没有则写 `none`）、失败假设及结果、下一项未决事项。主智能体整合证据并确认其与实际产物一致。专家验证已提供充分证据时，避免重复运行完整测试套件。
